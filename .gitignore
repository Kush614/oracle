node_modules
.next
.env
.env.local
.DS_Store
*.log
dist
coverage
.vercel
.turbo
.tmp
.tsbuildinfo
tsconfig.tsbuildinfo
# Both .npmrc variants carry a Chainguard pull token — never commit.
.npmrc
.npmrc.chainguard
# Cosmo Router binary — 87 MB, fetched via `wgc router download-binary`
infra/cosmo/bin/
# wgc local cache of composed supergraph
oracle-default/
